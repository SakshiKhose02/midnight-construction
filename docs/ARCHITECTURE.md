# System Architecture

## Overview Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                     MIDNIGHT CONSTRUCTION                        │
│                  Database Integration System                     │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────┐          ┌──────────────────────┐
│   CUSTOMER SIDE      │          │    ADMIN SIDE        │
│  (Public Access)     │          │  (Authenticated)     │
└──────────────────────┘          └──────────────────────┘
         │                                  │
         ▼                                  ▼
┌──────────────────────┐          ┌──────────────────────┐
│  get-quotation.html  │          │ admin-dashboard.html │
│  + CSS + JavaScript  │          │  + CSS + JavaScript  │
└──────────────────────┘          └──────────────────────┘
         │                                  │
         │ HTTP POST                        │ HTTP GET/POST
         │ (with file)                      │ (JSON)
         ▼                                  ▼
┌─────────────────────────────────────────────────────────┐
│                    PHP BACKEND                           │
│  ┌─────────────────────┐  ┌─────────────────────────┐  │
│  │ submit-quotation.php│  │    admin-api.php        │  │
│  │  - Validate input   │  │  - Authentication       │  │
│  │  - Handle upload    │  │  - Get quotations       │  │
│  │  - Save to DB       │  │  - Update status        │  │
│  └─────────────────────┘  │  - Add notes            │  │
│                            │  - Delete quotations    │  │
│         │                  │  - Get statistics       │  │
│         │                  └─────────────────────────┘  │
│         │                              │                 │
│         │           ┌──────────────────┘                │
│         │           │                                    │
│         ▼           ▼                                    │
│  ┌────────────────────────────────────────────┐        │
│  │            config.php                       │        │
│  │  - Database connection (PDO)                │        │
│  │  - Security configuration                   │        │
│  │  - Helper functions                         │        │
│  └────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                  MYSQL DATABASE                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  quotations                                     │    │
│  │  - Customer requests                            │    │
│  │  - Budget, project details                      │    │
│  │  - Status tracking                              │    │
│  │  - File references                              │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  admin_users                                    │    │
│  │  - Admin credentials (hashed passwords)         │    │
│  │  - Login history                                │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  activity_log                                   │    │
│  │  - Audit trail (future use)                     │    │
│  └────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│                   FILE STORAGE                           │
│                   php/uploads/                           │
│  - Architectural plans (PDF, JPG, PNG)                  │
│  - Unique filenames                                      │
│  - Protected directory                                   │
└─────────────────────────────────────────────────────────┘
```

## Data Flow Diagrams

### Customer Quotation Submission Flow

```
Customer fills form
       │
       ▼
JavaScript validation
       │
       ▼
Submit to submit-quotation.php (POST)
       │
       ├─→ Validate all inputs
       │   └─→ Invalid? Return error (400)
       │
       ├─→ Handle file upload (if provided)
       │   ├─→ Check file type (PDF/JPG/PNG)
       │   ├─→ Check file size (< 10MB)
       │   └─→ Save with unique name
       │
       ├─→ Connect to database (PDO)
       │   └─→ Error? Return error (500)
       │
       ├─→ Insert into quotations table
       │   └─→ Error? Return error (500)
       │
       └─→ Return success (201)
           └─→ Show success modal to customer
```

### Admin Dashboard Authentication Flow

```
Admin opens dashboard
       │
       ▼
Check authentication (check-auth)
       │
       ├─→ Not authenticated?
       │   └─→ Show login screen
       │       │
       │       ▼
       │   Enter credentials
       │       │
       │       ▼
       │   POST to admin-api.php?action=login
       │       │
       │       ├─→ Validate credentials
       │       │   └─→ Invalid? Return error
       │       │
       │       ├─→ Create session
       │       │
       │       └─→ Return user data
       │
       └─→ Authenticated?
           └─→ Load dashboard
               ├─→ Get statistics
               ├─→ Get recent quotations
               └─→ Display overview
```

### Admin View Quotations Flow

```
Admin clicks "Quotations" tab
       │
       ▼
GET admin-api.php?action=get-quotations
       │
       ├─→ Check authentication
       │   └─→ Not authenticated? Return 401
       │
       ├─→ Parse filters (status, search, page)
       │
       ├─→ Query database with filters
       │   ├─→ Apply WHERE clauses
       │   ├─→ Apply pagination
       │   └─→ Get total count
       │
       └─→ Return quotations + pagination data
           └─→ Render table in dashboard
               └─→ Admin can:
                   ├─→ View details (modal)
                   ├─→ Update status
                   ├─→ Add notes
                   └─→ Delete quotation
```

## Security Architecture

```
┌─────────────────────────────────────────────┐
│           SECURITY LAYERS                    │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│  Layer 1: Input Validation                  │
│  - Client-side (JavaScript)                 │
│  - Server-side (PHP)                        │
└─────────────────────────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│  Layer 2: SQL Injection Prevention          │
│  - PDO Prepared Statements                  │
│  - Parameter binding                        │
└─────────────────────────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│  Layer 3: XSS Prevention                    │
│  - Input sanitization                       │
│  - Output escaping                          │
│  - htmlspecialchars()                       │
└─────────────────────────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│  Layer 4: Authentication                    │
│  - Password hashing (bcrypt)                │
│  - Session management                       │
│  - Timeout (1 hour)                         │
└─────────────────────────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│  Layer 5: File Upload Security              │
│  - Type validation (whitelist)              │
│  - Size limit (10MB)                        │
│  - Unique filenames                         │
│  - Protected directory                      │
└─────────────────────────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│  Layer 6: HTTPS (Production)                │
│  - Encrypted transmission                   │
│  - Certificate validation                   │
└─────────────────────────────────────────────┘
```

## Status Workflow

```
┌───────────┐
│  PENDING  │ ◄── New quotation created
└─────┬─────┘
      │
      ▼
┌───────────┐
│ CONTACTED │ ◄── Admin reached out to customer
└─────┬─────┘
      │
      ▼
┌───────────┐
│  QUOTED   │ ◄── Quotation sent to customer
└─────┬─────┘
      │
      ├────────────────┐
      │                │
      ▼                ▼
┌───────────┐    ┌───────────┐
│ COMPLETED │    │ CANCELLED │
└───────────┘    └───────────┘

Note: Any status can move to CANCELLED at any time
```

## File Structure Map

```
midnight-construction/
│
├── 🌐 Frontend (Customer)
│   ├── html/get-quotation.html
│   ├── css/get-quotation.css
│   └── js/get-quotation.js
│
├── 🔐 Frontend (Admin)
│   ├── html/admin-dashboard.html
│   ├── css/admin-dashboard.css
│   └── js/admin-dashboard.js
│
├── ⚙️ Backend (PHP)
│   ├── php/config.php
│   ├── php/submit-quotation.php
│   ├── php/admin-api.php
│   └── php/setup.php
│
├── 💾 Database
│   └── database/setup.sql
│
├── 📁 Storage
│   ├── php/uploads/    (uploaded files)
│   └── php/logs/       (error logs)
│
└── 📚 Documentation
    ├── docs/DATABASE_SETUP.md
    ├── docs/QUICKSTART.md
    ├── docs/TESTING.md
    ├── docs/PROJECT_SUMMARY.md
    └── README.md
```

## Technology Stack

```
┌─────────────────────────────────────┐
│         TECHNOLOGY STACK             │
└─────────────────────────────────────┘

Frontend:
├── HTML5        (Structure)
├── CSS3         (Styling + Animations)
└── JavaScript   (Vanilla, ES6+)

Backend:
├── PHP 7.4+     (Server logic)
└── PDO          (Database layer)

Database:
└── MySQL 5.7+   (Data storage)

Server:
├── Apache       (Web server)
└── .htaccess    (Configuration)

Security:
├── bcrypt       (Password hashing)
├── PDO          (SQL injection prevention)
└── Sanitization (XSS prevention)

Icons:
└── Font Awesome 6
```

## Deployment Architecture

```
┌────────────────────────────────────────────┐
│            PRODUCTION SERVER               │
│                                            │
│  ┌──────────────────────────────────┐    │
│  │        Apache/Nginx               │    │
│  │  ┌─────────────────────────┐     │    │
│  │  │   HTTPS (SSL/TLS)       │     │    │
│  │  │  Port 443               │     │    │
│  │  └──────────┬──────────────┘     │    │
│  └─────────────┼────────────────────┘    │
│                │                          │
│  ┌─────────────▼────────────────────┐    │
│  │     PHP Application              │    │
│  │  - submit-quotation.php          │    │
│  │  - admin-api.php                 │    │
│  │  - config.php                    │    │
│  └─────────────┬────────────────────┘    │
│                │                          │
│  ┌─────────────▼────────────────────┐    │
│  │    MySQL Database                │    │
│  │  - midnight_construction DB      │    │
│  │  - 3 tables                      │    │
│  │  - Regular backups               │    │
│  └──────────────────────────────────┘    │
│                                            │
│  ┌──────────────────────────────────┐    │
│  │      File Storage                │    │
│  │  - uploads/ (protected)          │    │
│  │  - logs/ (protected)             │    │
│  └──────────────────────────────────┘    │
└────────────────────────────────────────────┘
```

---

**Legend:**
- ▼ = Data flow direction
- ├─→ = Conditional branch
- └─→ = Result/Action
- │ = Connection
